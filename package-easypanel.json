{
  "name": "agrana",
  "version": "1.0.0",
  "scripts": {
    "start": "serve -s www -p $PORT --cors --single",
    "build": "echo 'Build already done'",
    "heroku-postbuild": "echo 'Using pre-built files'"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": "18.x"
  }
}
