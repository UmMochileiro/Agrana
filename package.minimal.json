{
  "name": "agrana",
  "version": "1.0.0",
  "scripts": {
    "start": "serve -s www -p ${PORT:-4200} --cors --single",
    "serve": "serve -s www"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
